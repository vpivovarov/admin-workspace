{
  "name": "admin-workspace",
  "version": "1.0.0",
  "description": "Demo project to show our skills",
  "main": "index.js",
  "scripts": {
    "init-db": "openssl rand -base64 32 > ./backend/db/password.txt",
    "start-fe": "npm run start --prefix ./frontend",
    "start": "docker compose up --build",
    "init-fe": "npm i --prefix ./frontend",
    "init-ws": "npm i",
    "init": "npm-run-all init-ws init-fe init-db",
    "prepare": "husky"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/vpivovarov/admin-workspace.git"
  },
  "keywords": [
    "admin",
    "console",
    "demo"
  ],
  "author": "vpivovarov",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/vpivovarov/admin-workspace/issues"
  },
  "homepage": "https://github.com/vpivovarov/admin-workspace#readme",
  "devDependencies": {
    "husky": "9.0.10",
    "lint-staged": "^15.2.2",
    "npm-run-all": "^4.1.5"
  },
  "lint-staged": {
    "*.ts": [
      "npx prettier --write --ignore-unknown",
      "npx eslint --ext .ts --fix"
    ]
  }
}
